# Nebius Token Factory API token (used only for local CLI/testing — the web app
# takes the token at runtime via ⚙️ Settings and stores it in the browser only).
# Get yours at https://tokenfactory.nebius.com
API_TOKEN=your-nebius-token-here
